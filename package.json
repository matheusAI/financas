{
  "name": "financas-site",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "build":       "node dist/build.js",
    "serve":       "npx serve . --listen 4321 --no-clipboard",
    "test":        "playwright test",
    "test:ui":     "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug":  "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "dotenv": "^16.4.5"
  }
}
