# ── Credenciais e segredos ──
.env
.env.*
!.env.example

# ── Playwright ──
.auth/
playwright-report/
test-results/

# ── Node ──
node_modules/

# ── Scripts de setup local ──
download-supabase.js

# ── Segredos locais ──
settings.local.json
CLAUDE.md

# ── Artefatos de build (gerados pelo Next.js/Vercel e por npm run build:assets) ──
.next/
out/
public/dist/all.min.css
public/dist/app.min.js
